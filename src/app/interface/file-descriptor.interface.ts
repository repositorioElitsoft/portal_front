// file-descriptor.interface.ts
export interface FileDescriptor {
url: any;
    name: string;
    uploadDate: string; // O Date, si estás seguro de que el formato de fecha será compatible
    // ... cualquier otra propiedad que los objetos de archivo puedan tener
  }
  